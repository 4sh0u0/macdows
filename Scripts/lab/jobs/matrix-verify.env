PROGRAM='||powershell'
CMDARGS='-NoProfile -ExecutionPolicy Bypass -File \\tsclient\lab\tsallowlist-matrix-verify.ps1'
# 120, not the 240 of the first live run: run 4 measured the whole verify -- host-agent start,
# icon work, report write -- at 32s, so 120 is a ~3.7x margin rather than a ~7.5x guess.
TIMEOUT=120
