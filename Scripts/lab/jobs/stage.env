# Enforce-window form: '||<alias>' resolves only while the TS allow-list publication from the
# matrix Enforce step exists; on the restored host use stage-path.env.
PROGRAM='||powershell'
CMDARGS='-NoProfile -ExecutionPolicy Bypass -File \\tsclient\lab\stage.ps1'
TIMEOUT=30
