PROGRAM='||powershell'
CMDARGS='-NoProfile -ExecutionPolicy Bypass -File \\tsclient\lab\server-snapshot.ps1'
# Bound: winver + CIM + six registry keys are sub-second; the event-log section reads up to 500
# newest events from each RDS-related channel (typically 10-15 channels) and is the only part
# whose wall clock scales with host history. The report is checkpointed after every channel, so
# a timeout still leaves every finished section on the drive.
TIMEOUT=240
