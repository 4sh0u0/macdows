<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>0.0.1</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<!-- The reason string macOS shows in the local-network permission dialog. Added by
	     adr/0019 §2 R-6 lane T1, which is the last change before the first unattended run:
	     the ONE permission this app needs is Local Network (a grep of App/ and Packages/
	     finds no Accessibility, Screen Recording or Input Monitoring API anywhere), the
	     dialog appears on the first real connection, and until now App/project.yml's own
	     comment claimed this plist "carries real usage-description keys" while it carried
	     none -- so the dialog would have offered no reason at all. Deliberately generic:
	     no host name, no address and no account appears here, because this file is
	     tracked. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>Macdows connects to a Windows PC on your local network over RDP.</string>
	<key>NSHumanReadableCopyright</key>
	<string>Copyright 2026 Haru Hazawa.</string>
</dict>
</plist>
